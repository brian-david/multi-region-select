import React from "react";

function DynamicCanvas(){
   const zones = [];

   

   return (
      <React.Fragment>
         <canvas id="dynamicCanvas">

         </canvas>
      </React.Fragment>
   );
}

export default DynamicCanvas